# 대시보드 화면 코드 반입 — 사전 체크리스트

> 대상: 기획자 (대시보드 화면을 로컬에서 개발 완료한 상태)
> 목적: 로컬 코드를 vca 저장소로 가져오기 전에, 이식 작업량을 미리 가늠하기 위한 사전 조사.
> **작성 방법: 이 문서를 화면을 개발한 클로드 세션에 붙여넣고 "프로젝트를 확인해서 아래 답변 칸을 채워줘"라고 하면 됩니다.** 직접 채우셔도 되고요 — 대부분 프로젝트 루트의 `package.json`만 보면 답이 나옵니다.

## 답변 칸

### 1. 프레임워크 / 빌드 도구
- [ ] Vite (React)
- [ ] Next.js
- [ ] Create React App
- [ ] 기타: ___________
- React 버전 (`package.json`의 `react`): ___________
- TypeScript 사용 여부: 예 / 아니오

### 2. 지도
- 사용한 지도 라이브러리 (`package.json`에서 map 관련 항목):
  - [ ] Mapbox (`mapbox-gl`, `react-map-gl`)
  - [ ] Leaflet (`leaflet`, `react-leaflet`)
  - [ ] Google Maps (`@vis.gl/react-google-maps` 등)
  - [ ] 기타: ___________
- API 키/토큰이 필요한가요? 예 / 아니오 (필요하다면 어디에 넣었는지: `.env` / 코드 직접 / 기타)
- 지도 타일(배경 지도)의 출처: ___________ (예: Mapbox 기본, OpenStreetMap, CARTO 등)

### 3. 차트 (Detection Topology & Average 그래프)
- 사용한 라이브러리:
  - [ ] Recharts
  - [ ] Chart.js
  - [ ] ECharts
  - [ ] D3 직접
  - [ ] 기타: ___________

### 4. 스타일
- [ ] Tailwind CSS
- [ ] CSS Modules
- [ ] styled-components / Emotion
- [ ] 플레인 CSS
- [ ] UI 킷 사용 (MUI, Ant Design 등): ___________
- 폰트·아이콘 출처 (구글 폰트 / 유료 폰트 / 아이콘 라이브러리 이름): ___________

### 5. 데이터와 상태
- 화면에 보이는 데이터(감지 목록, 카운터, 지도 라벨 등)는 지금 어디서 오나요?
  - [ ] 코드에 하드코딩된 목(mock) 데이터
  - [ ] 별도 JSON 파일
  - [ ] fetch/axios로 어딘가 호출 (어디: ___________)
  - [ ] MSW 등 목 서버
- 전역 상태 라이브러리 사용 여부 (Zustand, Redux, Jotai 등): ___________
- 라우팅 사용 여부 (react-router 등, 페이지가 여러 개인지): ___________

### 6. 기타
- `.env` 파일이 있나요? 있다면 어떤 키들이 들어 있나요 (값 말고 **키 이름만**): ___________
- 직접 만들지 않고 외부에서 가져온 에셋(이미지·아이콘·데모 얼굴 사진 등)이 있다면 출처: ___________
- `package.json`의 `dependencies` 전체를 아래에 붙여넣어 주세요:

```json
(여기에 붙여넣기)
```

## 답변 후

답변을 백엔드 담당(박상훈)에게 전달해 주세요. 저장소 접근 권한을 드린 뒤, 로컬 프로젝트를 **있는 그대로** `import/dashboard-ui` 브랜치로 push하는 방법을 안내드립니다 (기존 코드와 섞이지 않으니 구조를 미리 바꾸실 필요 없습니다).

---

## (백엔드용) 판정 가이드

| 항목 | 답변 | 이식 작업량 영향 |
|---|---|---|
| 프레임워크 | Vite React | 그대로 이식 — 최소 |
| | Next.js | 컴포넌트만 적출, 라우팅·이미지 최적화 등 껍데기 재작업 — 중 |
| React | 19.x | 그대로 |
| | 18.x | 의존성 호환 확인 필요 — 소 |
| 지도 | Leaflet/OSM | 키 불필요, 온프레미스 유리 — 최소 |
| | Mapbox/Google | 토큰·과금·오프라인 납품 검토 필요 — **별도 결정 사항** |
| 데이터 | 하드코딩/JSON 목 | 계약 타입으로 매핑만 — 소 |
| | 컴포넌트 안 fetch | 데이터 접근 적출 필요 — 중 |
| 스타일 | Tailwind 등 일관 시스템 | 기존 스캐폴드가 백지라 그대로 수용 — 최소 |
| 상태관리 | Zustand | 기존 스택과 동일 — 최소 |
| 라우팅 | 단일 페이지 | 최소. 멀티 페이지면 features 분할 설계 |
