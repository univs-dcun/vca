# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the template is the only documentation of what can be configured — tile
# source, support contact, API base URL — and it holds no secrets. Without this
# exception it stayed untracked, so a fresh clone had no record of any setting.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
