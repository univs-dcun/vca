# VCA 프론트엔드 — 전달 문서

기준 스냅샷: **`import-snapshot-20260910`** (2026-09-10)

인증 화면, 관리 콘솔(Portal), 관제 앱이 화면으로는 전부 만들어져 있고, **서버 연동은
아직 하나도 되어 있지 않습니다.** 이 문서는 그것을 확인하는 방법과, 서버가 무엇을
해줘야 하는지를 가리킵니다.

지난 전달(`import-snapshot-20260909`) 이후 바뀐 것만 보시려면:

```bash
git diff import-snapshot-20260909 import-snapshot-20260910
# 53개 파일, +9211 / -2400
```

## 1. 실행

```bash
npm install
npm run dev          # http://localhost:3000
```

인터넷 없이도 전부 동작합니다. 백엔드도 필요 없습니다 — 데이터는 모두 목업입니다.

## 2. 플로우 문서

다이어그램 6개와 서버가 지켜야 할 계약이 여기 있습니다.

**https://claude.ai/code/artifact/f66095d5-40b2-4551-a145-44f133ea2065**

권한 모델(역할·권한 키 매트릭스)은 별도 문서입니다.

**https://claude.ai/code/artifact/228dc3d3-3f22-4a99-a844-ff61e5c0891d**

## 3. 이번 스냅샷에서 바뀐 것

### 새로 생긴 화면 (관리 콘솔)

`/portal` 좌측 메뉴와 프로젝트 상세 탭에서 들어갑니다.

| 화면 | 파일 | 무엇 |
|---|---|---|
| 설정 | `PortalSettingsPage.tsx` | 계정·프로젝트·팀 메일(SMTP)·명단 분류·조회 목적·표시 언어 |
| 변경 기록 | `ProjectActivityTab.tsx` | 감사 로그 — 기간·담당자·검색·CSV 내보내기 |
| 조회 기록 | `ProjectSearchLogTab.tsx` | 조회 사유의 읽는 쪽. **1차 배포에서는 꺼져 있습니다** |
| 명단 분류 | `WatchlistCategories.tsx` | 기관이 직접 정의 — 색·기본 유효기간·근거 필수 여부 |

### 같이 들어온 구조 변경 (포털·앱 양쪽)

- **권한 두 축 분리** — 포털 역할(`permission`)과 앱 접근(`appAccess`)이 별개입니다.
  Auditor는 실제로 읽기 전용으로 동작합니다.
- **타임존은 프로젝트 단위** — `Project.timeZone`. 날짜·"오늘"은 보는 사람이 아니라
  **현장**의 시간대를 따릅니다. 앱의 화면 표시는 전환이 끝났습니다 — 헤더 시계, 검출
  카드의 캡처 시각, "N분 전", 경과 시간이 모두 `Project.timeZone`을 지납니다.
  남아 있는 고정 호출은 **목업 시드 생성기 한 곳**뿐이고 그것은 의도적입니다
  (`recentSgtStamp` 계열 — 모듈 로드 시점에 만들어지므로 현장이 정해지기 전입니다.
  이유는 `src/lib/time.ts`의 "mock stamp loop" 주석에 있습니다). 실제 검출이 들어오면
  그 시드와 함께 사라집니다.
- **한국어** — 포털·앱 양쪽. 화면 언어는 포털과 앱이 **각각** 기억합니다.
  데이터(카메라 이름·인물명)는 번역하지 않습니다.
- **배포 단위 스위치** — `src/lib/complianceConfig.ts`. 조회 사유 요구 여부 등,
  기관마다 다른 것을 여기 한 곳에서 끕니다.

### 관제 앱 쪽 변경 — 서버 계약에 영향이 있는 것

관제 앱(DASHBOARD / DATA / REDMAP / BEST FRAME)을 한 차례 점검하면서, 화면이 사실이
아닌 것을 사실처럼 말하던 지점들을 고쳤습니다. 그 과정에서 **서버가 무엇을 줘야 하는지가
바뀐 것이 세 가지** 있습니다.

**1. 카메라 목록이 유일한 출처입니다 — 목업 필러를 지웠습니다.**

전에는 한 화면에 카메라 수가 세 개 있었습니다: 지도의 지역 필은 등록부 59대, 사이드바
장비 목록과 가동률 도넛은 1,040대(등록부 + 목업 필러 981), 지도를 확대하면 찍히는 점은
또 다른 1,029대. 셋 다 "이 현장의 카메라 수"를 자칭했고, 필러 981대는 어느 등록부에도
없어서 그중 OFFLINE로 뜨는 84대는 **아무도 손댈 수 없는 행**이었습니다.

필러를 삭제했습니다(`src/lib/mockData.ts`의 `devices` 1,000행). 이제
`src/lib/api/dashboard.ts`가 카메라 등록부 하나를 `Device` 모양으로 투영하고,
장비 목록·가동률·지도 점·지역 필·헤더의 구동 상태가 전부 같은 목록을 읽습니다.

> 서버 쪽으로: 카메라 목록 API가 그대로 이 화면들의 모집단이 됩니다. 등록된 카메라
> 수가 곧 화면에 보이는 수이고, 별도의 "전체 배포 규모" 같은 값은 쓰지 않습니다.
> 규모감을 위해 목업을 1,000대로 부풀려 두었던 것은 의도적으로 버린 결정입니다
> (2026-09-10).

**2. 검출은 날짜를 함께 보내야 합니다.**

`Detection`(`src/types/detection.ts`)에 `date`가 생겼습니다. 전에는 검출이 벽시계
문자열(`"22:56:00"`)만 들고 있었고, 화면이 거기에 **오늘 날짜**를 붙여 표시했습니다.
현장 시각이 00:30일 때 94분 전 검출은 어제 22:56인데, 오늘 날짜와 짝지어져 약 23시간
**미래**로 표시됐습니다. 증거 화면에서 그랬습니다.

> 서버 쪽으로: 검출/프레임 응답의 시각은 날짜를 포함해야 합니다(ISO instant면 가장
> 좋고, 벽시계 쌍이면 `{date, time}`으로). 시각만 주면 화면이 날짜를 추측할 수밖에
> 없고, 추측이 틀리는 구간이 자정 앞뒤로 최대 하루입니다.

**3. 세션 응답에 계정 상태가 필요합니다.**

`canEnterApp`(관리자가 앱을 부여했는가)과 `canUseAppNow`(지금 들어갈 수 있는가)를
나눴습니다(`src/lib/vcaStore.ts`). 전에는 앱의 문이 `appAccess`만 봤기 때문에
`status: "suspended", appAccess: true`인 계정(시드의 `david.ong`이 정확히 이 조합)은
로그인에서는 막히고 앱 문은 통과했습니다.

> 서버 쪽으로: 세션/로그인 응답에 권한 플래그와 **계정 상태**가 함께 있어야 합니다.
> 근무 중에 관리자가 계정을 정지시키면 화면이 내려가야 하는데, 상태 없이는 그 판단을
> 할 수 없습니다.

**4. 이동 경로의 각 지점이 자기 좌표를 들고 와야 합니다.**

한 사람이 여러 카메라에 잡히면 앱은 그 목격들을 하나의 이동 경로로 접습니다
(`TrackingHop`). 그 지점들이 좌표를 잃고 있었습니다 — 스토어에 쓸 때 버리고, 읽을 때
**마지막 목격 위치를 전 구간에 복사**했습니다. 노베나→게일랑→베독으로 움직인 경로의 앞
두 지점이 전부 베독에 찍혔고, 지도가 지역별로 세는 숫자가 거기서 틀어졌습니다.

이름으로 카메라를 되찾는 우회로는 쓰지 않습니다. 해석에 실패하면 조용히 첫 번째 카메라로
떨어져서, 목격을 4~5km 떨어진 지역에 귀속시킵니다.

> 서버 쪽으로: 경로/재식별 응답의 각 목격이 **그 목격의 카메라와 좌표**를 함께 실어야
> 합니다. 경로 하나에 좌표 하나(최신 지점)만 오면, 화면은 그 경로를 지도에 그릴 수도
> 지역별로 셀 수도 없습니다 — 이름으로 되찾는 것은 위의 이유로 쓰지 않습니다.

**함께: 카메라 시각 편차가 화면에 드러납니다.**

경과 시간이 음수로 계산되면(카메라나 서버 시계가 이쪽보다 앞선 경우) "시각 어긋남"이라고
표시합니다. 전에는 0초로 깎아서 "방금"으로 보여줬는데, 그것은 운영자가 손쓸 수 없는
시계 문제를 정상으로 위장하는 표시였습니다. 레드맵의 경로는 서로 다른 카메라의 검출을
시각 순으로 잇기 때문에, **편차를 아는 것**이 필요합니다 — 정밀 동기화가 아니라, 얼마나
어긋나 있는지를 받을 수 있는지가 질문입니다.

**5. 채널 한도를 프로비저닝 호출도 거절해야 합니다.**

화면은 이제 채널 수가 기록되지 않은 프로젝트에 소스를 붙이지 못하게 막습니다. 전에는
한도 미기록을 **무제한**으로 읽었고, 프로젝트를 만드는 마법사가 라이선스 필드를 하나도
쓰지 않아서 마법사로 만든 현장은 전부 무제한이었습니다(시드 4개는 한도가 있어 드러나지
않았습니다).

> 서버 쪽으로: 프론트가 막는 것은 예의 쪽 절반입니다. **카메라 등록과 프로젝트 생성
> 양쪽을 API가 거절**해야 진짜로 닫힙니다 — 포털을 한 번도 열지 않은 클라이언트에는
> 화면의 판단이 닿지 않습니다. 그리고 그 채널 수가 **설치 단위인지 프로젝트 단위인지**가
> 먼저 정해져야 합니다. 지금 프론트는 프로젝트에 붙은 값으로 그리고 있습니다.

**6. 열람기록은 프로젝트와 함께 지우면 안 됩니다.**

프로젝트를 삭제할 때 "누가 누구를 언제 어떤 사유로 조회했는지"까지 같이 지우고 있었습니다.
고쳤습니다 — 감사 로그를 남기는 것과 같은 이유이고, 얼굴 인식 제품에서는 그 이유가 더
강합니다. 사이트를 닫는 순간이 바로 그 기록을 없애고 싶은 때이기도 합니다.

> 서버 쪽으로: 삭제를 **cascade로 걸지 말아 주세요.** 그리고 남은 절반이 있습니다 —
> 프로젝트가 사라지면 읽을 화면이 없습니다(조회 기록 화면이 projectId로 거릅니다).
> 팀 단위 조회 뷰, 또는 이 기록의 보관기간을 얼마로 할지가 함께 정해져야 합니다.

**나머지 (서버 계약과 무관)**: 목업 데이터가 카메라를 스스로 발명하지 않도록 Re-ID ·
Redmap · Best Frame · RedFace 네 화면을 카메라 등록부에 묶었습니다. 무엇이 발견됐는지는
목업이 쓰고, 어디서 찍혔는지는 등록부가 답합니다 — 그래서 헤더에서 현장을 바꾸면 네 화면이
실제로 그 현장의 카메라만 말합니다. 한국어 미번역 85곳, 키보드로 닿지 않던 주 동작 3곳도
함께 처리했습니다.

## 4. 로그인

**아무 이메일 + 아무 비밀번호**로 통과합니다. 비밀번호는 비어있지 않은지만 봅니다.

시드 계정 중 셋은 실제로 다르게 동작합니다:

| 아이디 | 결과 |
|---|---|
| `grace.tan@univs.ai` | 최고관리자(owner) → Portal 로 진입 |
| `david.ong@univs.ai` | 정지된 계정 — 로그인 차단 |
| `wei.chen@univs.ai` | 미활성 계정 — 로그인 차단 |

이 두 줄에 딸린 문제를 하나 열어 둡니다. 지금 화면은 **비밀번호와 무관하게** 이 상태를
알려줍니다 — 아무 문자 하나를 비밀번호로 넣고 사번을 바꿔 가며 눌러 보면 어느 주소·사번이
계정이고 어떤 상태인지 알 수 있습니다(사번은 `EMP-20xx` 연속값입니다). 목업에는 비밀번호
검증이 없어서 이 화면 혼자서는 고칠 수 없습니다.

> 서버 쪽으로: 계정 상태 안내는 **자격 증명 검증에 성공한 뒤에만** 나와야 합니다.
> 실패는 어느 쪽이 틀렸는지 말하지 않는다는 계약이 이미 코드에 있습니다
> (`src/lib/authErrors.ts`). 응답이 그 구분을 지켜 주면 화면은 그대로 따릅니다.

## 5. 실패 화면 보는 방법

이 상태들은 **정상 조작으로 도달할 수 없습니다.** 그것을 만들어내는 서버 응답이 아직
없기 때문입니다. 주소로 직접 여세요.

### 로그인
```
/login?demo=badCredentials      아이디 또는 비밀번호 불일치
/login?demo=locked              연속 실패로 잠김
/login?demo=suspended           관리자가 접근 회수
/login?demo=pendingApproval     승인 대기
/login?demo=notActivated        코드만 받고 비밀번호 미설정
/login?demo=sessionExpired      세션 만료
/login?demo=signedOut           로그아웃됨
```

### 비밀번호 재설정
```
/forgot-password                     이메일 입력
/forgot-password?demo=code           코드 입력
/forgot-password?demo=wrong          코드 불일치
/forgot-password?demo=expired        만료
/forgot-password?demo=throttled      시도 초과 (입력 차단)
/forgot-password?demo=resendLimit    재발송 한도
/forgot-password?demo=password       새 비밀번호
/forgot-password?demo=done           완료
/forgot-password?demo=adminOnly      메일 서버 없는 배포
```

### 가입 (등록 코드)
```
/register                  아래 코드를 넣어보세요
/register?demo=throttled   시도 초과 (입력 차단)
```

| 코드 | 결과 |
|---|---|
| `BPXW-4762` | 정상 (Priya Nair) |
| `QRST-5678` | 이미 사용된 코드 |
| `VXYZ-2345` | 만료된 코드 |

### 관리자 쪽
```
/portal                      Users & Permissions 탭 → 행 메뉴
/portal?recovery=adminOnly   메일 서버가 없는 상태
```

## 6. 지금 동작하지 않는 것

오해를 막기 위해 명시합니다.

- **비밀번호 검증** — 비어있지 않은지만 봅니다
- **세션** — 토큰도 쿠키도 만들지 않습니다
- **라우트 보호** — 로그인하지 않아도 주소를 치면 대시보드가 열립니다. `/portal`도
  마찬가지입니다. 신원은 세션이 아니라 목업 상수(`SIGNED_IN_USER`)이고, 그 주소가 어느
  계정에도 없으면 **아무것도 잠기지 않습니다**(fail-open — 데모가 계속 열려 있도록).
  화면 안의 권한 분기는 **표시용**이며, 진짜 문은 서버여야 합니다.

  단, 신원이 계정과 일치할 때는 화면이 실제로 거부합니다. 서버가 같은 판단을 다시
  해야 하는 지점들입니다:

  | 화면 | 무엇을 검사하나 |
  |---|---|
  | 앱 전체 (`/`, `/mypage`) | `appAccess` **그리고** 계정 status. 거부되면 아무것도 렌더하지 않고 이동합니다 |
  | Re-ID · RedFace · Redmap | `appSearch`. 화면 전체가 막힙니다(탭 버튼·딥링크·카드 버튼 어느 입구로 들어와도) |
  | `/signup` · `/request-access` | `src/lib/authConfig.ts`의 배포 스위치. 꺼져 있으면 `/login`으로 |
  | `/password-setup?reason=temp` | 그 계정이 실제로 임시 비밀번호 상태인지 + 발급 후 24시간 이내인지 |
- **계정 생성** — 가입해도 계정이 만들어지지 않습니다
- **메일 발송** — 아무것도 보내지 않습니다. 팀 메일(SMTP) 화면도 저장만 하고
  릴레이에 접속하지 않습니다
- **계정 잠금 / 유휴 로그아웃** — 없습니다
- **감사 로그** — 브라우저 메모리(store)에만 쌓이고 새로고침하면 사라집니다
- **업로드 영상 분석** — 큐에 `pending`으로 들어간 뒤 **아무것도 진행시키지 않습니다**
- **VIP 얼굴 사진 등록 / CSV 일괄 등록** — 전부 브라우저 안에서만 처리됩니다

코드에서 `HANDOFF` 로 검색하면 "여기에 어떤 호출이 들어가야 하는지"가 **65곳**에
주석으로 적혀 있습니다.

## 7. 설정

`.env.example` 을 `.env.local` 로 복사해서 씁니다. 지도 타일 출처, 지원 연락처,
API 주소가 거기서 나옵니다.

| 파일 | 무엇을 켜고 끄나 |
|---|---|
| `src/lib/authConfig.ts` | 배포별 인증 스위치 (비밀번호 복구 방식 등) |
| `src/lib/complianceConfig.ts` | 조회 사유 요구 여부 등 기관별 준수 항목 |

## 8. 이 스냅샷과 모노레포 `frontend/` 의 관계

이 저장소는 모노레포의 `frontend/` 와 **다른 앱**입니다.

| | 이 스냅샷 | `main:frontend/` |
|---|---|---|
| 빌드 | Next.js 16 | Vite + react-router |
| 데이터 | 목업 store (zustand) | orval 생성 훅 + axios + MSW |
| 실시간 | 없음 | `mqtt` ^5.15.2 |

`import/*` 브랜치는 main 과 조상을 공유하지 않으므로 섞이지 않습니다. 화면을 **있는
그대로** 넘기는 원본이고, 최종적으로 화면 코드가 살 곳은 `frontend/src/features` ·
`components` 입니다 (CODEOWNERS 기준 기획자 소유).

`docs/checklist-frontend-import.md` 의 이식 비용 판정은 **DASHBOARD / BEST FRAME /
DATA / REDMAP 네 화면**을 기준으로 매겨진 것입니다. 이번 스냅샷은 그보다 넓습니다 —
Portal 관리 콘솔 전체와 인증 화면 전부가 들어 있습니다.

### 회신 3번에 대한 정정

2026-09-09 회신 3번이 "앱 대시보드는 브라우저가 EMQX WebSocket을 직접 구독하고 기존
`mqttClient` 가 재접속을 처리한다"고 되어 있는데, **그것은 이 앱이 아니라
`main:frontend/` 입니다** — `mqtt` 의존성이 거기 있고, 이 Next.js 앱에는 mqtt /
websocket 클라이언트가 없습니다(자리를 표시한 주석 한 줄뿐).

카메라가 죽은 것을 몇 초 안에 아는지가 여기 달려 있으므로, **어느 프론트 기준으로
이야기하는지** 부터 맞춰 주세요.
