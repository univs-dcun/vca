# VCA 프론트엔드 — 전달 문서

기준 스냅샷: **`portal-handoff-v3`** (2026-09-15)

인증 화면, 관리 콘솔(Portal), 관제 앱이 화면으로는 전부 만들어져 있고, **서버 연동은
아직 하나도 되어 있지 않습니다.** 이 문서는 그것을 확인하는 방법과, 서버가 무엇을
해줘야 하는지를 가리킵니다.

지난 전달(`portal-handoff-v2` = 그쪽 `import-snapshot-20260910`) 이후 바뀐 것만 보시려면:

```bash
git diff portal-handoff-v2 portal-handoff-v3
# 69개 파일, +8238 / -1542 (162 커밋)
```

**태그가 붙은 지점까지가 완성본입니다.** 태그 뒤의 커밋은 작업 중으로 보셔도 됩니다 —
그렇게 읽어달라는 요청을 받았고, 그에 맞춰 붙였습니다.

## 1. 실행

```bash
npm install
npm run dev          # http://localhost:3000
```

인터넷 없이도 전부 동작합니다. 백엔드도 필요 없습니다 — 데이터는 모두 목업입니다.

## 2. 플로우 문서

다이어그램 6개와 서버가 지켜야 할 계약이 여기 있습니다.

**https://claude.ai/code/artifact/f66095d5-40b2-4551-a145-44f133ea2065**

권한 모델(역할·권한 키 매트릭스)은 별도 문서입니다.

**https://claude.ai/code/artifact/228dc3d3-3f22-4a99-a844-ff61e5c0891d**

## 3. 이번 스냅샷에서 바뀐 것

### 새로 생긴 화면 (관리 콘솔)

`/portal` 좌측 메뉴와 프로젝트 상세 탭에서 들어갑니다.

| 화면 | 파일 | 무엇 |
|---|---|---|
| 설정 | `PortalSettingsPage.tsx` | 계정·프로젝트·팀 메일(SMTP)·표시 언어. VIP 분류와 조회 목적 항목은 스위치로 가려져 있습니다(§6) |
| 변경 기록 | `ProjectActivityTab.tsx` | 감사 로그 — 기간·담당자·검색·CSV 내보내기 |
| 조회 기록 | `ProjectSearchLogTab.tsx` | 누가 누구를 언제 찾았나. **1차 배포에서 켭니다** — 사유는 묻지 않으므로 사유 칸은 비어 있습니다(§6) |
| VIP 분류 | `VipCategories.tsx` | 기관이 직접 정의 — 색·기본 유효기간·근거 필수 여부. **1차에서는 꺼짐**(§6). 파일 이름이 `WatchlistCategories.tsx`에서 바뀌었습니다 — "와치리스트"와 "VIP"를 한 제품에서 둘 다 쓰지 않기로 했습니다 |
| 외부 요청 | `ProjectRequestsTab.tsx` | 영상 제공 요청과 삭제 요청의 대장. 접수 → 결정 → 제공/완료 |
| 카메라 일괄 가져오기 | `CameraImportModal.tsx` · `lib/cameraImport.ts` | 설치 첫날용 CSV 반입. 내보내기가 쓰는 열을 그대로 읽습니다 |
| 영상 보관기간 | 설정 화면의 프로젝트 섹션 | `Project.retentionDays`. 기본값 없음 |

### v3에서 새로 생긴 것

| 화면 · 기능 | 파일 | 무엇 |
|---|---|---|
| 접근 게이트 | `AccessGate.tsx` · `src/app/gate/` | 로그인 다음에 오는 화면 하나. 콘솔과 앱 둘 다 쓸 수 있는 계정에 **처음 한 번만** 어디로 갈지 묻고, 그 뒤로는 기억한 곳으로 바로 보냅니다. 거절 화면 네 종류(권한 없음·정지·앱 미허용·콘솔 미허용)가 여기 모여 있습니다 |
| 라이선스 파일 설치 | `lib/licenseFile.ts` · `ProjectLicenseTab.tsx` | 서명된 라이선스 파일을 읽어 채널 한도·만료를 설정합니다. **최고관리자 전용**(2026-09-14 결정) — 한도와 만료는 계약 사항이라 카메라 이름 고치는 일과 같은 칸에 둘 수 없습니다 |
| 증거 내보내기 | `lib/evidence.ts` | 프레임을 매니페스트와 함께 내보냅니다. `frame_sha256`은 브라우저가 찍으면 안 됩니다 — §3의 9번 항목 참고 |

권한 정책 두 가지가 이번에 확정됐습니다: **라이선스 설치는 최고관리자만**, 그리고
**앱 접근이 없는 계정에는 인물 검색을 줄 수 없습니다**(빼는 방향은 원래 막혀 있었고,
주는 방향이 열려 있었습니다). 권한 매트릭스 전체는 별도 문서로 보냅니다 — 재공유
요청받은 그 문서입니다.

프로젝트 상세의 좌측 레일은 세 묶음입니다 — **작업 공간**(개요·VIP 등록·입력 소스),
**관리**(서버 및 API·사용자 및 권한·라이선스), **기록**(변경 기록·조회 기록·외부 요청).

### 같이 들어온 구조 변경 (포털·앱 양쪽)

- **권한 두 축 분리** — 포털 역할(`permission`)과 앱 접근(`appAccess`)이 별개입니다.
  Auditor는 실제로 읽기 전용으로 동작합니다.
- **타임존은 프로젝트 단위** — `Project.timeZone`. 날짜·"오늘"은 보는 사람이 아니라
  **현장**의 시간대를 따릅니다. 앱의 화면 표시는 전환이 끝났습니다 — 헤더 시계, 검출
  카드의 캡처 시각, "N분 전", 경과 시간이 모두 `Project.timeZone`을 지납니다.
  남아 있는 고정 호출은 **목업 시드 생성기 한 곳**뿐이고 그것은 의도적입니다
  (`recentSgtStamp` 계열 — 모듈 로드 시점에 만들어지므로 현장이 정해지기 전입니다.
  이유는 `src/lib/time.ts`의 "mock stamp loop" 주석에 있습니다). 실제 검출이 들어오면
  그 시드와 함께 사라집니다.
- **한국어** — 포털·앱 양쪽. 화면 언어는 포털과 앱이 **각각** 기억합니다.
  데이터(카메라 이름·인물명)는 번역하지 않습니다.
- **배포 단위 스위치** — `src/lib/complianceConfig.ts`. 조회 사유 요구 여부 등,
  기관마다 다른 것을 여기 한 곳에서 끕니다.

### 관제 앱 쪽 변경 — 서버 계약에 영향이 있는 것

관제 앱(DASHBOARD / DATA / REDMAP / BEST FRAME)을 한 차례 점검하면서, 화면이 사실이
아닌 것을 사실처럼 말하던 지점들을 고쳤습니다. 그 과정에서 **서버가 무엇을 줘야 하는지가
바뀐 것이 일곱 가지** 있습니다. 공통된 모양이 하나 있어서 먼저 적어둡니다 — 일곱 건 중
다섯 건이 **에러가 아니라 그럴듯한 값**이었습니다. 빌드도 타입 검사도 통과하고, 화면은
자신 있게 틀립니다. 그래서 계약에 적을 것은 대개 값이 아니라 **경로**입니다: "좌표를
보내라"가 아니라 "이름으로 카메라를 되찾는 우회로는 쓰지 않는다"까지 써야 합니다.

**1. 카메라 목록이 유일한 출처입니다 — 목업 필러를 지웠습니다.**

전에는 한 화면에 카메라 수가 세 개 있었습니다: 지도의 지역 필은 등록부 59대, 사이드바
장비 목록과 가동률 도넛은 1,040대(등록부 + 목업 필러 981), 지도를 확대하면 찍히는 점은
또 다른 1,029대. 셋 다 "이 현장의 카메라 수"를 자칭했고, 필러 981대는 어느 등록부에도
없어서 그중 OFFLINE로 뜨는 84대는 **아무도 손댈 수 없는 행**이었습니다.

필러를 삭제했습니다(`src/lib/mockData.ts`의 `devices` 1,000행). 이제
`src/lib/api/dashboard.ts`가 카메라 등록부 하나를 `Device` 모양으로 투영하고,
장비 목록·가동률·지도 점·지역 필·헤더의 구동 상태가 전부 같은 목록을 읽습니다.

> 서버 쪽으로: 카메라 목록 API가 그대로 이 화면들의 모집단이 됩니다. 등록된 카메라
> 수가 곧 화면에 보이는 수이고, 별도의 "전체 배포 규모" 같은 값은 쓰지 않습니다.
> 규모감을 위해 목업을 1,000대로 부풀려 두었던 것은 의도적으로 버린 결정입니다
> (2026-09-10).

**2. 검출은 날짜를 함께 보내야 합니다.**

`Detection`(`src/types/detection.ts`)에 `date`가 생겼습니다. 전에는 검출이 벽시계
문자열(`"22:56:00"`)만 들고 있었고, 화면이 거기에 **오늘 날짜**를 붙여 표시했습니다.
현장 시각이 00:30일 때 94분 전 검출은 어제 22:56인데, 오늘 날짜와 짝지어져 약 23시간
**미래**로 표시됐습니다. 증거 화면에서 그랬습니다.

> 서버 쪽으로: 검출/프레임 응답의 시각은 날짜를 포함해야 합니다(ISO instant면 가장
> 좋고, 벽시계 쌍이면 `{date, time}`으로). 시각만 주면 화면이 날짜를 추측할 수밖에
> 없고, 추측이 틀리는 구간이 자정 앞뒤로 최대 하루입니다.

**3. 세션 응답에 계정 상태가 필요합니다.**

`canEnterApp`(관리자가 앱을 부여했는가)과 `canUseAppNow`(지금 들어갈 수 있는가)를
나눴습니다(`src/lib/vcaStore.ts`). 전에는 앱의 문이 `appAccess`만 봤기 때문에
`status: "suspended", appAccess: true`인 계정(시드의 `david.ong`이 정확히 이 조합)은
로그인에서는 막히고 앱 문은 통과했습니다.

> 서버 쪽으로: 세션/로그인 응답에 권한 플래그와 **계정 상태**가 함께 있어야 합니다.
> 근무 중에 관리자가 계정을 정지시키면 화면이 내려가야 하는데, 상태 없이는 그 판단을
> 할 수 없습니다.

**4. 이동 경로의 각 지점이 자기 좌표를 들고 와야 합니다.**

한 사람이 여러 카메라에 잡히면 앱은 그 목격들을 하나의 이동 경로로 접습니다
(`TrackingHop`). 그 지점들이 좌표를 잃고 있었습니다 — 스토어에 쓸 때 버리고, 읽을 때
**마지막 목격 위치를 전 구간에 복사**했습니다. 노베나→게일랑→베독으로 움직인 경로의 앞
두 지점이 전부 베독에 찍혔고, 지도가 지역별로 세는 숫자가 거기서 틀어졌습니다.

이름으로 카메라를 되찾는 우회로는 쓰지 않습니다. 해석에 실패하면 조용히 첫 번째 카메라로
떨어져서, 목격을 4~5km 떨어진 지역에 귀속시킵니다.

> 서버 쪽으로: 경로/재식별 응답의 각 목격이 **그 목격의 카메라와 좌표**를 함께 실어야
> 합니다. 경로 하나에 좌표 하나(최신 지점)만 오면, 화면은 그 경로를 지도에 그릴 수도
> 지역별로 셀 수도 없습니다 — 이름으로 되찾는 것은 위의 이유로 쓰지 않습니다.

**확인만 필요한 질문: 지역 귀속을 서버가 합니까.**

9월 9일에 드린 16문 중 이 항목만 회신에 없었습니다. 저희 쪽 예상은 **"한다"**입니다 —
맞는지만 알려주시면 됩니다.

지금 프론트는 카메라 좌표를 받아 **가장 가까운 구역 중심점**으로 귀속시킵니다. 경계는
비교하지 않습니다 — 구역마다 점이 하나뿐이라 "안에 있는지"를 물을 수 있는 데이터가
아닙니다. 경계선 근처 카메라는 옆 구역으로 갑니다. **지도의 구역별 숫자는 고객이 카메라
증설 근거로 윗선에 올리는 자료**라, 귀속이 틀리면 틀린 곳에 카메라를 세웁니다.

> 서버 쪽으로 두 가지입니다.
>
> 하나, 검출 응답에 **구역 id를 실어 주세요**(`locationId` 또는 그에 해당하는 필드).
> 오면 그 값이 이깁니다 — 프론트는 **계산을 멈추고 읽기만 합니다.** 둘 다 계산해서 비교하는
> 형태로 두지 않습니다. 같은 질문에 답이 둘 있으면 화면마다 다른 답이 나오고, 그건 고치는
> 데 세 번 걸립니다.
>
> 둘, **경계 데이터는 도시마다 따로 있어야 합니다.** 이 제품은 도시 단위 온프레미스
> 설치라 한 도시의 구역 목록을 다른 도시가 쓸 수 없습니다.
>
> 이건 가정이 아닙니다. **몽골 배포가 확정되어 있습니다**(2026-09-11 확인). 지금 프론트의
> 구역 목록은 싱가포르 17개가 상수로 박혀 있습니다 — 프론트를 싱가포르를 예시로 만들었기
> 때문이고, 울란바토르 구역 데이터는 저희가 만들어낼 수 있는 것이 아닙니다. **그 목록은
> 서버가 설치별로 줘야 합니다**(구역 id·표시명·경계 또는 중심 좌표). 백엔드가 이걸 상수로
> 들고 시작하면 양쪽이 각자 싱가포르를 박게 되고, 그때는 고치는 자리가 둘입니다.

**몽골 배포에서 같이 따라오는 것들** (확정 2026-09-11)

| | 지금 상태 |
|---|---|
| 위도 보정 | **됨.** 구역 귀속이 `cos(위도)`를 쓰므로 도시를 안 가립니다. 울란바토르는 북위 47.9°로 경도 1도가 위도 1도의 67%인데(싱가포르 99.97%), 이 보정이 없으면 귀속이 동서로 끌립니다 |
| 타임존 | **됨.** `Project.timeZone`을 따라갑니다 — `Asia/Ulaanbaatar`. 함수 이름의 `sgt` 접두어는 옛 이름일 뿐 동작은 현장 기준입니다 |
| 지도 타일 | **됨.** `NEXT_PUBLIC_MAP_TILE_URL`로 설치별 지정. 인터넷 없는 현장이라 고객 GIS 서버나 동봉 타일을 가리킵니다 |
| 구역 목록 | **서버 필요.** 위 참조 |
| 지도 초기 중심 | 싱가포르 좌표가 상수입니다. 설치 설정으로 뺄 자리 |
| 키릴 이름 | 표 정렬 콜레이터가 `ko`로 고정되어 있습니다. 결정적으로 정렬되므로 깨지지는 않지만 몽골어 정렬 규칙은 아닙니다 |

**함께: 카메라 시각 편차가 화면에 드러납니다.**

경과 시간이 음수로 계산되면(카메라나 서버 시계가 이쪽보다 앞선 경우) "시각 어긋남"이라고
표시합니다. 전에는 0초로 깎아서 "방금"으로 보여줬는데, 그것은 운영자가 손쓸 수 없는
시계 문제를 정상으로 위장하는 표시였습니다. 레드맵의 경로는 서로 다른 카메라의 검출을
시각 순으로 잇기 때문에, **편차를 아는 것**이 필요합니다 — 정밀 동기화가 아니라, 얼마나
어긋나 있는지를 받을 수 있는지가 질문입니다.

**5. 채널 한도를 프로비저닝 호출도 거절해야 합니다.**

화면은 이제 채널 수가 기록되지 않은 프로젝트에 소스를 붙이지 못하게 막습니다. 전에는
한도 미기록을 **무제한**으로 읽었고, 프로젝트를 만드는 마법사가 라이선스 필드를 하나도
쓰지 않아서 마법사로 만든 현장은 전부 무제한이었습니다(시드 4개는 한도가 있어 드러나지
않았습니다).

포털 쪽도 같은 한도를 집행합니다. 카메라 CSV 일괄 가져오기에서 남은 채널을 넘는 행은
거부되고 어느 코드가 거부됐는지 화면에 나옵니다. 한도가 **기록되지 않은** 프로젝트는
무제한이 아니라 "라이선스 없음"으로 읽습니다.

> 서버 쪽으로: 프론트가 막는 것은 예의 쪽 절반입니다. **카메라 등록과 프로젝트 생성
> 양쪽을 API가 거절**해야 진짜로 닫힙니다 — 포털을 한 번도 열지 않은 클라이언트에는
> 화면의 판단이 닿지 않습니다. 그리고 그 채널 수가 **설치 단위인지 프로젝트 단위인지**가
> 먼저 정해져야 합니다. 지금 프론트는 프로젝트에 붙은 값으로 그리고 있습니다.

**6. 열람기록은 프로젝트와 함께 지우면 안 됩니다.**

프로젝트를 삭제할 때 "누가 누구를 언제 어떤 사유로 조회했는지"까지 같이 지우고 있었습니다.
고쳤습니다 — 감사 로그를 남기는 것과 같은 이유이고, 얼굴 인식 제품에서는 그 이유가 더
강합니다. 사이트를 닫는 순간이 바로 그 기록을 없애고 싶은 때이기도 합니다.

> 서버 쪽으로: 삭제를 **cascade로 걸지 말아 주세요.** 그리고 남은 절반이 있습니다 —
> 프로젝트가 사라지면 읽을 화면이 없습니다(조회 기록 화면이 projectId로 거릅니다).
> 팀 단위 조회 뷰, 또는 이 기록의 보관기간을 얼마로 할지가 함께 정해져야 합니다.

**7. 검출은 사람마다 안정된 id를 실어야 합니다 — 이름으로 사람을 되찾지 않습니다.**

지도의 지역 필이 "카메라 몇 대에서 몇 명"을 말하게 하려면 검출에 사람 식별자가 필요한데,
`VcaEvent.personId`는 이름만 그럴듯하고 **그 자리가 아니었습니다.** 두 writer가 전부
검출(sighting)의 id를 넣고 있어서, distinct를 세면 **검출 수가 "명"이라는 단어를 입고
나옵니다.** 첫 판이 두 명이 일곱 번 찍힌 구역에 "7명"이라고 적었고, 브라우저에서 잡았습니다.

지금은 이름(`personName`)으로 셉니다. 이름은 id보다 약한 신원입니다 — 동명이인 둘이
1명으로 합쳐지고 **명수가 조용히 낮게** 나옵니다. 목업 이름이 전부 달라서 지금은 맞고,
그것이 맞는 이유의 전부입니다.

> 서버 쪽으로: 검출 응답에 **사람마다 안정된 id**가 있어야 합니다. 그리고 계약에 적을
> 것은 값이 아니라 **경로입니다** — "이름으로 사람을 되찾는 우회로는 쓰지 않는다"까지
> 써야 합니다. 4번의 좌표와 같은 모양의 요구입니다: 식별자가 안 오면 프론트가 이름으로
> 때우고, 해석 실패가 조용한 오귀속이 됩니다.
>
> 이름 정리도 함께: 지금 `personId`가 담고 있는 값 자체는 일관되게 "이 sighting의 id"라서
> **값이 아니라 이름이 틀렸습니다.** 서버를 붙일 때 `sightingId`로 바꾸고, 사람 식별자는
> 별도 필드로 받는 것이 맞는 정리입니다.

이 필드 혼동이 이미 한 곳에서 조용한 결함이었습니다. 프로젝트 삭제가 `Person.id`를
`VcaEvent.personId`와 대조하고 있었는데 두 id 공간이 겹칠 수 없어 **조건이 영원히 참**,
즉 **현장을 삭제해도 그 현장 검출이 하나도 안 지워졌습니다.** 카메라 소속 기준으로
바꿨습니다(`projectIdForCameraId` — 화면이 무엇을 보여줄지 정할 때 쓰는 그 함수라,
지워지는 것과 보일 수 있는 것이 갈라질 수 없습니다).

> 서버 쪽으로: 검출의 보관 주체는 **카메라**입니다. 그리고 6번의 열람기록과 방향이 반대인
> 것이 의도입니다 — 열람기록은 **운영자가 한 일**이라 책임 근거로 남기고, 검출은 **시민이
> 어디 있었는지**라 목적이 끝나면 들고 있을 이유가 없습니다. 신원 미상 검출도 같이
> 갑니다(끝내 누구인지 확인하지 못했다는 점에서 오히려 더 그렇습니다). 얼굴 인식 제품에서
> 이 둘을 한 규칙으로 다루면 둘 중 하나는 반드시 틀립니다.

**나머지 (서버 계약과 무관)**: 목업 데이터가 카메라를 스스로 발명하지 않도록 Re-ID ·
Redmap · Best Frame · RedFace 네 화면을 카메라 등록부에 묶었습니다. 무엇이 발견됐는지는
목업이 쓰고, 어디서 찍혔는지는 등록부가 답합니다 — 그래서 헤더에서 현장을 바꾸면 네 화면이
실제로 그 현장의 카메라만 말합니다. 한국어 미번역 85곳, 키보드로 닿지 않던 주 동작 3곳도
함께 처리했습니다.

### 관리 콘솔 쪽에서 더해진 계약 (같은 목록의 계속)

**8. 영상 보관기간은 기록이지 집행이 아닙니다.**

설정 화면에서 현장별 보관기간(일)을 정할 수 있게 했습니다. 조례·계약서·개인정보
처리방침에 들어가는 숫자인데 제품에 정하는 자리가 없어서 레코더를 세팅한 사람
머릿속에 있었습니다. 기본값은 두지 않았습니다 — 30일은 추측이고, 추측된 보관기간은
고객이 정당화할 수 없는 초과보관이거나 아무도 동의하지 않은 삭제입니다.

> 서버 쪽으로: **프론트는 프레임을 하나도 지우지 않습니다.** 이 값이 레코더와 서버의
> 만료 작업에 닿아야 효력이 생깁니다. 그 경로가 생기기 전까지 화면은 "정하는 것이지
> 집행하는 것이 아니다"라고 소리내어 말합니다. 변경은 이전 값과 함께 감사에 남습니다 —
> "3월에는 며칠 보관하고 있었나"가 민원과 소송이 묻는 질문이고, 현재값만으로는 답이
> 안 나옵니다.

**9. 증거 파일의 프레임 해시는 서버 몫입니다.**

검출 한 건을 메타데이터와 관제요원 판단과 함께 파일로 내보내는 기능이 생겼습니다
(`src/lib/evidence.ts`). 매니페스트는 **자기 자신을 해싱**하고, `frame_sha256`은
의도적으로 `null`입니다.

> 서버 쪽으로: 브라우저가 망에서 받은 사본을 해싱하면 **촬영이 아니라 사본을** 증명합니다.
> 증거용 해시는 프레임이 기록되는 자리(카메라, 또는 그것을 받은 서버)에서 찍혀 프레임과
> 함께 이동해야 합니다. 여기서 계산해 "frame hash"라고 이름 붙이면 출처처럼 보이는데
> 아닙니다. 그리고 **추출 자체가 서버 호출이어야** 합니다 — 지금은 브라우저가 자기
> 감사 줄을 쓰는데, 그건 스스로에게 남기는 메모이지 기록이 아닙니다.

**10. 검출의 `category`와 사람의 판단은 같은 필드가 아닙니다.**

실측한 서버의 `category`가 `vip · staff · unauthorized · vehicle · unknown · false_positive`
여섯 종이고, **`false_positive`가 `category`의 값 중 하나**입니다. 화면에도 같은 단어가
있는데 뜻이 다릅니다: 앱의 판단 기록(`DetectionJudgement`)은 **관제요원이 이 검출을 보고
아니라고 한 것**이고, 누가·언제까지 함께 남습니다. 서버의 것은 **검출기 자신의 분류**입니다.

두 가지가 걸립니다.

- 그대로 읽어 화면에 태우면 **아무도 안 본 검출이 사람이 확인한 것처럼 보입니다.** 기계의
  추정이 사람의 서명을 달고 나오는 셈이고, 이건 화면이 자신 있게 틀리는 그 모양입니다.
- `category`가 배타적이라 `false_positive`가 붙는 순간 **그 검출의 `vip`과 신뢰도가
  사라집니다.** 즉 "오탐일 수 있는 VIP 검출"을 표현할 자리가 없습니다 — 사람이 판단하려면
  판단 대상의 신원이 남아 있어야 하는데, 판단하기 전에 지워집니다.

> 서버 쪽으로: **분리해 주세요.** `category`는 무엇이 찍혔는지(`vip`/`vehicle`/…)에 두고,
> 검출기의 오탐 의심은 별도 필드(`suspectedFalsePositive` 같은 불리언 또는 점수)로 주십시오.
> 사람의 판단은 또 다른 필드이고, **누가·언제가 없으면 판단이 아닙니다.**

### 답을 기다리는 것 (정책)

**프로젝트 삭제 — 아직 못 정했습니다.** 보류 이유가 "되돌릴 수 없는 동작이라 정책이
먼저 필요하다"는 것이었고, 맞습니다. 무엇을 지울지는 정해져 있습니다(카메라·업로드·
서버·VIP 명단·직원 명부·조회 기록은 지우고, **변경 기록과 계정은 남깁니다** — 사이트를
지워도 사이트에 무엇을 했는지의 기록은 남아야 하고, 계정은 팀 소속이라 프로젝트만
목록에서 빠집니다). 남은 것은 **언제** 지우는가 하나입니다 — 즉시 삭제인지, 유예 기간을
두고 그 사이 되돌릴 수 있게 하는지. 정해서 따로 알리겠습니다. 팀 삭제는 이 결정에
딸려 갑니다.

외부 요청 대장의 삭제 요청 화면은 이 제품이 한 사람에 대해 들고 있는 **여덟 범주**를
늘어놓고 각각 지움/보존/미정을 표시합니다. 여섯은 정해져 있고(명단은 지움, 조회 기록·
영상 제공 기록·변경 기록은 보존, 내보낸 파일·녹화 영상은 콘솔 소관 아님), **둘은
미정으로 남겨 두었습니다** — 검출 이력과 그에 대한 판단. 지우면 시스템이 무엇을 했는지가
사라지고, 남기면 잊혀지길 요청한 사람의 생체정보 파생 기록을 계속 갖게 됩니다. 제품이
고를 문제가 아니라 기관이 정할 문제라, 화면은 미정이 하나라도 있으면 완료로 닫히지
않습니다.

## 4. 로그인

**아무 이메일 + 아무 비밀번호**로 통과합니다. 비밀번호는 비어있지 않은지만 봅니다.

시드 계정 중 셋은 실제로 다르게 동작합니다:

| 아이디 | 결과 |
|---|---|
| `grace.tan@univs.ai` | 최고관리자(owner) → Portal 로 진입 |
| `david.ong@univs.ai` | 정지된 계정 — 로그인 차단 |
| `wei.chen@univs.ai` | 미활성 계정 — 로그인 차단 |

이 두 줄에 딸린 문제를 하나 열어 둡니다. 지금 화면은 **비밀번호와 무관하게** 이 상태를
알려줍니다 — 아무 문자 하나를 비밀번호로 넣고 사번을 바꿔 가며 눌러 보면 어느 주소·사번이
계정이고 어떤 상태인지 알 수 있습니다(사번은 `EMP-20xx` 연속값입니다). 목업에는 비밀번호
검증이 없어서 이 화면 혼자서는 고칠 수 없습니다.

> 서버 쪽으로: 계정 상태 안내는 **자격 증명 검증에 성공한 뒤에만** 나와야 합니다.
> 실패는 어느 쪽이 틀렸는지 말하지 않는다는 계약이 이미 코드에 있습니다
> (`src/lib/authErrors.ts`). 응답이 그 구분을 지켜 주면 화면은 그대로 따릅니다.

## 5. 실패 화면 보는 방법

이 상태들은 **정상 조작으로 도달할 수 없습니다.** 그것을 만들어내는 서버 응답이 아직
없기 때문입니다. 주소로 직접 여세요.

### 로그인
```
/login?demo=badCredentials      아이디 또는 비밀번호 불일치
/login?demo=locked              연속 실패로 잠김
/login?demo=suspended           관리자가 접근 회수
/login?demo=pendingApproval     승인 대기
/login?demo=notActivated        코드만 받고 비밀번호 미설정
/login?demo=sessionExpired      세션 만료
/login?demo=signedOut           로그아웃됨
```

### 비밀번호 재설정
```
/forgot-password                     이메일 입력
/forgot-password?demo=code           코드 입력
/forgot-password?demo=wrong          코드 불일치
/forgot-password?demo=expired        만료
/forgot-password?demo=throttled      시도 초과 (입력 차단)
/forgot-password?demo=resendLimit    재발송 한도
/forgot-password?demo=password       새 비밀번호
/forgot-password?demo=done           완료
/forgot-password?demo=adminOnly      메일 서버 없는 배포
```

### 가입 (등록 코드)
```
/register                  아래 코드를 넣어보세요
/register?demo=throttled   시도 초과 (입력 차단)
```

| 코드 | 결과 |
|---|---|
| `BPXW-4762` | 정상 (Priya Nair) |
| `QRST-5678` | 이미 사용된 코드 |
| `VXYZ-2345` | 만료된 코드 |

### 관리자 쪽
```
/portal                      Users & Permissions 탭 → 행 메뉴
/portal?recovery=adminOnly   메일 서버가 없는 상태
```

## 6. 이번 릴리스에서 끈 것 — **목업 그대로 배선하지 마세요**

만든 뒤에 스위치로 내린 것들입니다. 화면도 스토어도 그대로 남아 있어서 **코드만 보면
살아 있는 기능처럼 보입니다.** 전부 `src/lib/complianceConfig.ts` 한 파일에 있고,
배포 시 `NEXT_PUBLIC_*`로 빼는 것이 핸드오프 항목입니다.

| 스위치 | 상태 | 무엇이 가려지나 |
|---|---|---|
| `externalRequests` | **끔** | 외부 요청 대장 — 레일 항목·탭 본문·`?tab=requests` 경로. 다음 스냅샷에 가져갈 계획이라고 적어주셨는데, **그 사이에 껐습니다** |
| `requireSearchPurpose` | **끔** | 검색 전에 운영자에게 사유를 묻는 게이트. 앱 쪽 게이트 화면은 코드에서 지웠고, 포털 설정의 목적 관리 화면만 남았습니다 |
| `searchAccessLog` | **켬** | ↑와 반대입니다. **조회 기록은 남깁니다** — 아래 별항 참고 |
| `personRelease` | **끔** | 인물 해제·복권. 첫 릴리스에서 명단에서 내리는 방법은 삭제 하나입니다 |
| `vipCategories` | **끔** | VIP 분류 — 등록 폼의 분류 칸, 표의 분류 열, CSV 열, 설정 레일 항목. 데이터 모델(`VipCategory`·`Person.categoryId`)은 유지, **계약은 쓰지 않기로 확정**(회신에서 동의받음) |
| `detectionJudgement` | **끔** | 검출 판단(맞음/아님). 판단이 새로고침에 사라지는데 화면은 "누가 언제 확인했다"를 찍고 있었습니다 — 서버가 받아줄 때 켭니다 |

### 조회 기록은 켰습니다 — 사유는 묻지 않고

묻는 것과 기록하는 것은 두 질문이고 답이 갈립니다. 묻는 것은 운영자가 매 검색마다
체감하는 마찰이고, 기록은 운영자에게 비용이 없으면서 감사가 가장 먼저 묻는 것입니다.
**그래서 사유 게이트는 끄고 기록만 켰습니다.**

- 기록되는 곳 넷: 레드맵 검색 · 레드맵에 이름을 들고 들어온 경우 · 실시간 관제 검색 ·
  Re-ID 분석 검색. **RedFace는 아직 안 했습니다** — 거기의 조회는 검색 버튼이 아니라
  기준 대상 선택이라 모양이 다릅니다
- 남는 것: 언제 · 누가 · 무엇으로 찾았나 · 어느 화면 · 결과 몇 건
- **사유는 비어 있습니다.** `SearchAccessRecord.purposeId`·`purposeLabel`을 선택 항목으로
  바꿨습니다 — 아무도 묻지 않았으니 적을 것이 없고, 채워 넣으면 운영자가 하지 않은
  주장이 기록에 들어갑니다
- **검색한 이미지는 기록하지 않습니다.** 그쪽이 해시만 남긴다고 하신 것과 같은 판단입니다 —
  브라우저가 받은 사본을 해싱하면 촬영이 아니라 사본을 증명합니다

## 7. 지금 동작하지 않는 것

오해를 막기 위해 명시합니다.

- **비밀번호 검증** — 비어있지 않은지만 봅니다
- **세션** — 토큰도 쿠키도 만들지 않습니다
- **라우트 보호** — 로그인하지 않아도 주소를 치면 대시보드가 열립니다. `/portal`도
  마찬가지입니다. 신원은 세션이 아니라 목업 상수(`SIGNED_IN_USER`)이고, 그 주소가 어느
  계정에도 없으면 **아무것도 잠기지 않습니다**(fail-open — 데모가 계속 열려 있도록).
  화면 안의 권한 분기는 **표시용**이며, 진짜 문은 서버여야 합니다.

  단, 신원이 계정과 일치할 때는 화면이 실제로 거부합니다. 서버가 같은 판단을 다시
  해야 하는 지점들입니다:

  | 화면 | 무엇을 검사하나 |
  |---|---|
  | 앱 전체 (`/`, `/mypage`) | `appAccess` **그리고** 계정 status. 거부되면 아무것도 렌더하지 않고 이동합니다 |
  | Re-ID · RedFace · Redmap | `appSearch`. 화면 전체가 막힙니다(탭 버튼·딥링크·카드 버튼 어느 입구로 들어와도) |
  | `/signup` · `/request-access` | `src/lib/authConfig.ts`의 배포 스위치. 꺼져 있으면 `/login`으로 |
  | `/password-setup?reason=temp` | 그 계정이 실제로 임시 비밀번호 상태인지 + 발급 후 24시간 이내인지 |
- **계정 생성** — 가입해도 계정이 만들어지지 않습니다
- **메일 발송** — 아무것도 보내지 않습니다. 팀 메일(SMTP) 화면도 저장만 하고
  릴레이에 접속하지 않습니다
- **계정 잠금 / 유휴 로그아웃** — 없습니다
- **감사 로그 · 조회 기록** — 브라우저 메모리(store)에만 쌓이고 새로고침하면 사라집니다.
  조회 기록은 이제 검색할 때마다 실제로 쌓입니다 — 받을 곳이 서버에 이미 있다고 하셨으니
  (`GET /admin/api/search-audit`, 보존 1년) 그쪽으로 보내면 됩니다
- **업로드 영상 분석** — 큐에 `pending`으로 들어간 뒤 **아무것도 진행시키지 않습니다**
- **VIP 얼굴 사진 등록 / CSV 일괄 등록** — 전부 브라우저 안에서만 처리됩니다

코드에서 `HANDOFF` 로 검색하면 "여기에 어떤 호출이 들어가야 하는지"가 **65곳**에
주석으로 적혀 있습니다.

## 8. 설정

`.env.example` 을 `.env.local` 로 복사해서 씁니다. 지도 타일 출처, 지원 연락처,
API 주소가 거기서 나옵니다.

| 파일 | 무엇을 켜고 끄나 |
|---|---|
| `src/lib/authConfig.ts` | 배포별 인증 스위치 (비밀번호 복구 방식 등) |
| `src/lib/complianceConfig.ts` | 조회 사유 요구 여부 등 기관별 준수 항목 |

## 9. 이 스냅샷과 모노레포 `frontend/` 의 관계

이 저장소는 모노레포의 `frontend/` 와 **다른 앱**입니다.

| | 이 스냅샷 | `main:frontend/` |
|---|---|---|
| 빌드 | Next.js 16 | Vite + react-router |
| 데이터 | 목업 store (zustand) | orval 생성 훅 + axios + MSW |
| 실시간 | 없음 | `mqtt` ^5.15.2 |

`import/*` 브랜치는 main 과 조상을 공유하지 않으므로 섞이지 않습니다. 화면을 **있는
그대로** 넘기는 원본이고, 최종적으로 화면 코드가 살 곳은 `frontend/src/features` ·
`components` 입니다 (CODEOWNERS 기준 기획자 소유).

`docs/checklist-frontend-import.md` 의 이식 비용 판정은 **DASHBOARD / BEST FRAME /
DATA / REDMAP 네 화면**을 기준으로 매겨진 것입니다. 이번 스냅샷은 그보다 넓습니다 —
Portal 관리 콘솔 전체와 인증 화면 전부가 들어 있습니다.

### 회신 3번에 대한 정정

2026-09-09 회신 3번이 "앱 대시보드는 브라우저가 EMQX WebSocket을 직접 구독하고 기존
`mqttClient` 가 재접속을 처리한다"고 되어 있는데, **그것은 이 앱이 아니라
`main:frontend/` 입니다** — `mqtt` 의존성이 거기 있고, 이 Next.js 앱에는 mqtt /
websocket 클라이언트가 없습니다(자리를 표시한 주석 한 줄뿐).

카메라가 죽은 것을 몇 초 안에 아는지가 여기 달려 있으므로, **어느 프론트 기준으로
이야기하는지** 부터 맞춰 주세요.
