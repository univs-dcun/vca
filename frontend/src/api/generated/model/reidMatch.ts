/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * VCA API
 * 백엔드가 생성/관리하는 API 계약. 프론트는 orval(npm run gen:api)로 이 스펙에서 타입/훅을 생성한다.

실시간 데이터(카메라 상태 변화, VIP 감지 이벤트, 집계 카운터)는 이 API가 아닌 MQTT로 전달된다 — 채널 경계와 병합 규칙은 vca-mqtt-broker 레포의 SPEC.md 참고. REST는 스냅샷(페이징 목록·이력·그래프)을, MQTT는 그 이후의 변화분을 담당한다.

공통 규약:
- 모든 응답은 { success, data, message, code } envelope
- 페이징은 { content, page, size, totalElements } (page는 0부터)
- 시각은 ISO-8601 UTC, 날짜 파라미터의 기본값은 사이트 로컬(Asia/Singapore) 기준 오늘
- ID는 문자열: cameraId/locationId는 ^[a-z0-9-]{1,64}$ (MQTT 토픽 경로와 공유)
- similarity는 0~1 실수 (표시 포맷은 프론트 책임)
 * OpenAPI spec version: 0.9.0
 */
import type { LatLng } from './latLng';
import type { ReidMatchGender } from './reidMatchGender';
import type { ReidMatchAttributes } from './reidMatchAttributes';
import type { ReidMatchMatchedVip } from './reidMatchMatchedVip';

/**
 * Re-ID 검색 매치 1건 (계약 v1.7). targetId는 감지 eventId와 동일 값 — Track on Map(v1.4 이동 경로)·Analyze Frame(v1.5) 딥링크와 크롭 조회(/detections/{id}/snapshot·face)에 이 값을 그대로 사용. snapshotUrl/faceUrl은 <img src>로 직접 사용
 */
export interface ReidMatch {
  /** 감지 eventId와 동일 값 — 기존 ID 재사용 규칙 */
  targetId: string;
  /** @pattern ^[a-z0-9-]{1,64}$ */
  cameraId: string;
  cameraName: string;
  /** @pattern ^[a-z0-9-]{1,64}$ */
  locationId: string;
  location: LatLng;
  /** 목격 시각 — Analyze Frame 진입 분(minute) 계산의 기준 */
  capturedAt: string;
  /**
   * 검색 대상과의 유사도 — 정렬 키(내림차순)
   * @minimum 0
   * @maximum 1
   */
  similarity: number;
  /**
   * 모듈 추정 성별. 미추정이면 null
   * @nullable
   */
  gender?: ReidMatchGender;
  /**
   * 모듈 추정 나이(단일 정수) — "28yo" 포맷은 프론트 책임. 미추정이면 null
   * @minimum 0
   * @maximum 130
   * @nullable
   */
  age?: number | null;
  /** 전신 크롭 — GET /detections/{targetId}/snapshot */
  snapshotUrl: string;
  /**
   * 얼굴 크롭 — GET /detections/{targetId}/face. 얼굴 미검출이면 null
   * @nullable
   */
  faceUrl?: string | null;
  /** @nullable */
  attributes?: ReidMatchAttributes;
  /**
   * 등록 인물 DB 매칭 — null이면 미등록 인물
   * @nullable
   */
  matchedVip?: ReidMatchMatchedVip;
}
