/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * VCA API
 * 백엔드가 생성/관리하는 API 계약. 프론트는 orval(npm run gen:api)로 이 스펙에서 타입/훅을 생성한다.

실시간 데이터(카메라 상태 변화, VIP 감지 이벤트, 집계 카운터)는 이 API가 아닌 MQTT로 전달된다 — 채널 경계와 병합 규칙은 vca-mqtt-broker 레포의 SPEC.md 참고. REST는 스냅샷(페이징 목록·이력·그래프)을, MQTT는 그 이후의 변화분을 담당한다.

공통 규약:
- 모든 응답은 { success, data, message, code } envelope
- 페이징은 { content, page, size, totalElements } (page는 0부터)
- 시각은 ISO-8601 UTC, 날짜 파라미터의 기본값은 사이트 로컬(Asia/Singapore) 기준 오늘
- ID는 문자열: cameraId/locationId는 ^[a-z0-9-]{1,64}$ (MQTT 토픽 경로와 공유)
- similarity는 0~1 실수 (표시 포맷은 프론트 책임)
 * OpenAPI spec version: 0.7.0
 */
import type { ReidSearchPersonsGender } from './reidSearchPersonsGender';
import type { ReidSearchPersonsApparelItem } from './reidSearchPersonsApparelItem';
import type { ReidSearchPersonsPropsItem } from './reidSearchPersonsPropsItem';

export type ReidSearchPersonsParams = {
/**
 * 등록 인물 참조 검색 (VIP Quick Select). 지정 시 이미지 업로드 불필요
 */
vipId?: string;
/**
 * 검색 구간 시작 (화면 기본값 '최근 7일'은 프론트가 명시 전달)
 */
from?: string;
/**
 * 검색 구간 끝 (미지정 시 현재까지)
 */
to?: string;
/**
 * 유사도 임계값 (화면 30/50/70/90% → 0.3/0.5/0.7/0.9)
 * @minimum 0
 * @maximum 1
 */
similarity: number;
/**
 * 이 카메라의 감지만 검색
 * @pattern ^[a-z0-9-]{1,64}$
 */
cameraId?: string;
gender?: ReidSearchPersonsGender;
/**
 * 복장 필터(OR) — 화면 칩 매핑: Trousers→trousers, Shorts→shorts, Skirts→skirts, Short Sleeve→short_sleeve, Long Sleeve→long_sleeve
 */
apparel?: ReidSearchPersonsApparelItem[];
/**
 * 소지품 필터(OR) — 화면 칩 매핑: Backpack/Bag→bag, Hat→hat, Wearing Glasses→glasses
 */
props?: ReidSearchPersonsPropsItem[];
/**
 * @minimum 1
 * @maximum 20
 */
maxResults?: number;
};
