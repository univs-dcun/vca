/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * VCA API
 * 백엔드가 생성/관리하는 API 계약. 프론트는 orval(npm run gen:api)로 이 스펙에서 타입/훅을 생성한다.

실시간 데이터(카메라 상태 변화, VIP 감지 이벤트, 집계 카운터)는 이 API가 아닌 MQTT로 전달된다 — 채널 경계와 병합 규칙은 vca-mqtt-broker 레포의 SPEC.md 참고. REST는 스냅샷(페이징 목록·이력·그래프)을, MQTT는 그 이후의 변화분을 담당한다.

공통 규약:
- 모든 응답은 { success, data, message, code } envelope
- 페이징은 { content, page, size, totalElements } (page는 0부터)
- 시각은 ISO-8601 UTC, 날짜 파라미터의 기본값은 사이트 로컬(Asia/Singapore) 기준 오늘
- ID는 문자열: cameraId/locationId는 ^[a-z0-9-]{1,64}$ (MQTT 토픽 경로와 공유)
- similarity는 0~1 실수 (표시 포맷은 프론트 책임)
 * OpenAPI spec version: 0.12.0
 */
import type { AssociateTopCamera } from './associateTopCamera';
import type { AssociateMatchedVip } from './associateMatchedVip';
import type { AssociatePeakPeriod } from './associatePeakPeriod';

/**
 * 동반 감지 인물 1명 (계약 v1.8). Zone 배치(>100/10~99/<10)는 화면이 coCaptures로 분류한다
 */
export interface Associate {
  /** 백엔드 발급 인물 키 — /targets/associate-evidence 참조 키, 화면 표시 ID로도 사용. 응답 후 최소 1시간 유효 */
  associateId: string;
  /** 대표 동반 감지의 크롭 /api 상대경로 — <img src>로 직접 사용 */
  faceUrl: string;
  /**
   * 기간 내 동반 감지 총횟수 — 정렬 키(내림차순)이자 Zone 분류 기준
   * @minimum 1
   */
  coCaptures: number;
  /** 최다 동반 장소 — (v1.10) associate-evidence의 pattern.topLocation과 카메라·count가 같아야 한다 */
  topCamera: AssociateTopCamera;
  /** 기간 내 첫 동반 감지 시각 */
  firstSeenAt: string;
  /** 기간 내 마지막 동반 감지 시각 */
  lastSeenAt: string;
  /**
   * 동료가 등록 인물이면 매칭 정보 — null이면 미등록
   * @nullable
   */
  matchedVip?: AssociateMatchedVip;
  /**
   * (v1.10) 기간 내 동반 감지가 발생한 고유 장소 수 — Data grid Locations 컬럼
   * @minimum 1
   */
  locations: number;
  /** (v1.10) 최다 동반 시간대 — Data grid Peak time 컬럼 */
  peakPeriod: AssociatePeakPeriod;
}
