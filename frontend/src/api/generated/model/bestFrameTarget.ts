/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * VCA API
 * 백엔드가 생성/관리하는 API 계약. 프론트는 orval(npm run gen:api)로 이 스펙에서 타입/훅을 생성한다.

실시간 데이터(카메라 상태 변화, VIP 감지 이벤트, 집계 카운터)는 이 API가 아닌 MQTT로 전달된다 — 채널 경계와 병합 규칙은 vca-mqtt-broker 레포의 SPEC.md 참고. REST는 스냅샷(페이징 목록·이력·그래프)을, MQTT는 그 이후의 변화분을 담당한다.

공통 규약:
- 모든 응답은 { success, data, message, code } envelope
- 페이징은 { content, page, size, totalElements } (page는 0부터)
- 시각은 ISO-8601 UTC, 날짜 파라미터의 기본값은 사이트 로컬(Asia/Singapore) 기준 오늘
- ID는 문자열: cameraId/locationId는 ^[a-z0-9-]{1,64}$ (MQTT 토픽 경로와 공유)
- similarity는 0~1 실수 (표시 포맷은 프론트 책임)
 * OpenAPI spec version: 0.8.0
 */
import type { DetectionCategory } from './detectionCategory';
import type { BestFrameTargetGender } from './bestFrameTargetGender';
import type { BBox } from './bBox';
import type { BestFrameTargetMatchedVip } from './bestFrameTargetMatchedVip';
import type { BestFrameTargetAnalysis } from './bestFrameTargetAnalysis';

/**
 * 베스트 프레임 내 tracked 대상 1건 (계약 v1.5). targetId는 매체별 기존 ID 체계 재사용 — 카메라는 감지 eventId, 비디오는 v1.3 비디오 대상 targetId. Track on Map(v1.4) 요청에 이 값을 그대로 전달한다
 */
export interface BestFrameTarget {
  targetId: string;
  category: DetectionCategory;
  /** 표시명 — 등록 인물 이름 / 외형 요약 / Vehicle {plate} */
  label: string;
  /**
   * VIP group / Staff (부서) 등 — 없으면 null
   * @nullable
   */
  groupLabel?: string | null;
  /**
   * 인물 성별 — 미상·차량이면 null
   * @nullable
   */
  gender?: BestFrameTargetGender;
  /**
   * 등록 인물 매칭 유사도 — 미등록·차량이면 null
   * @minimum 0
   * @maximum 1
   * @nullable
   */
  confidence?: number | null;
  bbox: BBox;
  /**
   * 등록 인물 매칭 — AI Inspection Detail의 ENROLLED DB. 미등록이면 null
   * @nullable
   */
  matchedVip?: BestFrameTargetMatchedVip;
  /** 대상 크롭의 /api 상대경로 — <img src>로 직접 사용 */
  cropUrl: string;
  /**
   * AI 분석 태그 — 분석 전이면 null
   * @nullable
   */
  analysis?: BestFrameTargetAnalysis;
}
